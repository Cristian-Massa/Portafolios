import React from "react";
import styled from "styled-components";
import tw from "twin.macro";

const Carrousel = styled.div`
    ${tw`
    `}
`

export const CarrouselGallery = (porps) =>{
    return(        
        <Carrousel>
        </Carrousel>
    )
}